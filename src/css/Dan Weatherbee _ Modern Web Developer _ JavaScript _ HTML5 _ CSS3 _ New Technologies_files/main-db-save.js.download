/*
Written in a Object Oriented pattern OOP by Dan Weatherbee

 Save to Storage and pull data from it. ================================
*/

app.saveContentToDB = function() {
    /*
     Browser Local Storage.
    Stringify for local storage.
    */

    var siteContentStr = JSON.stringify(app.siteContentObj);
    // Add to localstorage.
    localStorage.setItem('ContentEdited', siteContentStr);
    // Create Edit button.
    // app.createSomething();
    app.createMainText1();

};